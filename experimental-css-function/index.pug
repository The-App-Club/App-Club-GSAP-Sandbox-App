doctype html
html(lang="ja")
  head
    title="rolling"
    link(rel="stylesheet", href="index.css")
    script(type='text/javascript')
  body
    - let n = 16;

    .wrap(style=`--n: ${n}`)
      - for(let i = 0; i < n; i++)
        .item(style=`--i: ${i}`)
